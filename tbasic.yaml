---
- hosts: all
  connection: local
  gather_facts: true
  collections:
    - ansible.netcommon
  tasks:
    - name: send configuration
      telnet:
        login_prompt: '[: ]'
        prompts:
        - '[>#]'        
        command:
        - config system global
        - set hostname LAB-TEST-Auto2
        - end
        - exit
